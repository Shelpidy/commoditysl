import { StyleSheet, Text, View } from "react-native";
import React from "react";

const ResportScreen = () => {
   return (
      <View>
         <Text>ResportScreen</Text>
      </View>
   );
};

export default ResportScreen;

const styles = StyleSheet.create({});
